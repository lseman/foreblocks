import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"DARTS Search Pipeline","description":"","frontmatter":{},"headers":[],"relativePath":"architecture/darts-pipeline.md","filePath":"architecture/darts-pipeline.md"}');
const _sfc_main = { name: "architecture/darts-pipeline.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="darts-search-pipeline" tabindex="-1">DARTS Search Pipeline <a class="header-anchor" href="#darts-search-pipeline" aria-label="Permalink to “DARTS Search Pipeline”">​</a></h1><p>This page maps the conceptual DARTS workflow onto the actual ForeBlocks modules.</p><h2 id="why-this-pipeline-exists" tabindex="-1">Why this pipeline exists <a class="header-anchor" href="#why-this-pipeline-exists" aria-label="Permalink to “Why this pipeline exists”">​</a></h2><p>ForeBlocks does not treat DARTS as a single opaque training call. The search stack is deliberately staged so you can:</p><ul><li>prune bad candidates early with cheap signals</li><li>spend short differentiable-search budgets only on promoted candidates</li><li>derive a discrete architecture explicitly</li><li>retrain the final model from scratch when desired</li></ul><p>That structure makes it easier to debug, benchmark, and reason about the search than a monolithic NAS loop.</p><h2 id="phase-to-module-map" tabindex="-1">Phase-to-module map <a class="header-anchor" href="#phase-to-module-map" aria-label="Permalink to “Phase-to-module map”">​</a></h2><table tabindex="0"><thead><tr><th>Phase</th><th>Main code location</th><th>Role</th></tr></thead><tbody><tr><td>search-space definition</td><td><code>foreblocks/darts/config.py</code></td><td>defines architecture modes, op pools, and search budgets</td></tr><tr><td>candidate generation</td><td><code>foreblocks/darts/search/orchestrator.py</code></td><td>samples candidate configs and coordinates evaluation</td></tr><tr><td>zero-cost ranking</td><td><code>foreblocks/darts/search/zero_cost.py</code></td><td>computes cheap pre-training metrics</td></tr><tr><td>bilevel search training</td><td><code>foreblocks/darts/training/darts_loop.py</code></td><td>runs the mixed-architecture DARTS phase</td></tr><tr><td>multi-fidelity orchestration</td><td><code>foreblocks/darts/search/multi_fidelity.py</code></td><td>promotes top candidates and manages staged budgets</td></tr><tr><td>final fixed-model retraining</td><td><code>foreblocks/darts/training/final_trainer.py</code></td><td>retrains the derived architecture</td></tr><tr><td>result analysis</td><td><code>foreblocks/darts/evaluation/analyzer.py</code></td><td>builds search-result summaries and plots</td></tr><tr><td>public entry surface</td><td><code>foreblocks/darts/trainer.py</code></td><td>exposes the staged workflow as <code>DARTSTrainer</code> methods</td></tr></tbody></table><h2 id="public-control-flow" tabindex="-1">Public control flow <a class="header-anchor" href="#public-control-flow" aria-label="Permalink to “Public control flow”">​</a></h2><p>The high-level path is:</p><ol><li>instantiate <code>DARTSTrainer</code></li><li>call <code>multi_fidelity_search(...)</code></li><li>inspect the returned candidates and final metrics</li><li>save the best final model</li></ol><p>Internally, the call chain is roughly:</p><div class="language-text"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span>DARTSTrainer.multi_fidelity_search(...)</span></span>
<span class="line"><span>  -&gt; search/multi_fidelity.py</span></span>
<span class="line"><span>      -&gt; search/orchestrator.py</span></span>
<span class="line"><span>      -&gt; search/zero_cost.py</span></span>
<span class="line"><span>      -&gt; training/darts_loop.py</span></span>
<span class="line"><span>      -&gt; trainer.derive_final_architecture(...)</span></span>
<span class="line"><span>      -&gt; training/final_trainer.py</span></span></code></pre></div><h2 id="result-artifacts-by-phase" tabindex="-1">Result artifacts by phase <a class="header-anchor" href="#result-artifacts-by-phase" aria-label="Permalink to “Result artifacts by phase”">​</a></h2><h3 id="candidate-generation-and-zero-cost-ranking" tabindex="-1">Candidate generation and zero-cost ranking <a class="header-anchor" href="#candidate-generation-and-zero-cost-ranking" aria-label="Permalink to “Candidate generation and zero-cost ranking”">​</a></h3><p>Produces:</p><ul><li>raw candidates</li><li>cheap ranking metrics</li><li>promoted <code>top_k</code> candidates</li></ul><p>This is the best phase for debugging search-space quality.</p><h3 id="bilevel-darts-phase" tabindex="-1">Bilevel DARTS phase <a class="header-anchor" href="#bilevel-darts-phase" aria-label="Permalink to “Bilevel DARTS phase”">​</a></h3><p>Produces:</p><ul><li>mixed searched model</li><li>training and validation losses</li><li>alpha histories</li><li>intermediate metrics</li></ul><p>This is the best phase for debugging collapse, instability, or bad promotion thresholds.</p><h3 id="final-retraining-phase" tabindex="-1">Final retraining phase <a class="header-anchor" href="#final-retraining-phase" aria-label="Permalink to “Final retraining phase”">​</a></h3><p>Produces:</p><ul><li>the fixed <code>final_model</code></li><li>final metrics</li><li>training info</li></ul><p>This is the artifact that should matter most for downstream use.</p><h2 id="the-important-design-choice-fixed-model-retraining" tabindex="-1">The important design choice: fixed-model retraining <a class="header-anchor" href="#the-important-design-choice-fixed-model-retraining" aria-label="Permalink to “The important design choice: fixed-model retraining”">​</a></h2><p>ForeBlocks explicitly separates search-time mixed architectures from final deployment artifacts. That means the model you save after <code>derive_final_architecture(...)</code> is not just the same mixed model with a different label; it is a discrete architecture intended to be retrained and validated on its own merits.</p><p>This separation matters because:</p><ul><li>mixed-model performance can overestimate the final discrete architecture</li><li>retraining reveals whether the search signal really transfers</li><li>saving and sharing the final model becomes cleaner</li></ul><h2 id="stats-collection-and-benchmarking" tabindex="-1">Stats collection and benchmarking <a class="header-anchor" href="#stats-collection-and-benchmarking" aria-label="Permalink to “Stats collection and benchmarking”">​</a></h2><p>When <code>collect_stats=True</code>, the multi-fidelity pipeline stores timing and worker-parallelism artifacts. This is useful when you want to compare:</p><ul><li>how expensive phase 1 really is</li><li>whether more workers help in practice</li><li>whether candidate generation or search training is the true bottleneck</li></ul><p>Treat this as an experiment-benchmarking feature, not something you need for every search run.</p><h2 id="related-pages" tabindex="-1">Related pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to “Related pages”">​</a></h2><ul><li><a href="./../darts.html">DARTS Guide</a></li><li><a href="./../tutorials/darts-multifidelity-search.html">Run A DARTS Search</a></li><li><a href="./../reference/repository-map.html">Repository Map</a></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("architecture/darts-pipeline.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const dartsPipeline = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  dartsPipeline as default
};
