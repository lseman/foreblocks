"""foreblocks.layers.

Composable transformer and sequence-model building blocks.

Layers groups modular neural network components (embedding strategies, normalization
variants, graph convolutions, attention families, state-space models). These are
building blocks for ForecastingModel backbones, not end-to-end models—import
from subpackages (embeddings, norms, graph) to compose custom architectures.

"""

__all__: list[str] = []
